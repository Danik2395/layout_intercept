нужно создать массив internal_event_t для ждущих клавиш и поставить его в global_state_t

создать массив из fd через timerfd_create()
во время создания массива этого, нужно регестрировать дескрипторы черезе epoll_ctl()

когда нужно поставить что-то по таймеру, нужно использовать timerfd_settime(),
поставить структуру internal_event_t в жущий массив,
через epoll_ctl() поменять указатель в epoll_data на нужную структуру в ждущем массиве

когда пришло прерывание от epoll_wait(), нужно пройти по массиву epoll_events и смотреть на дескрипторы:
если это stdin, то нужно идти в одну ветку, если это другой дескриптор, нужно идти в другую
и получить internal_event_t из даты.

для главных функций:
    если на UP клавишу есть в pressed_state active, таймер убирать не нужно. нужно просто отправить UP из стейта.
    если нет в pressed_state active, нужно убрать таймер и отправить клавишу UP tap.

если пришло прерывание от таймера, нужно скопировать структуру из даты в event.
также нужно прочитать из того буфера, на который пришло прерывание, 64 бита.



нужно сделать очередь отправки, потому что в tap-hold логике нужно отправлять разом по две клавиши когда резолвиться как tap

---

1. prev_key_time ставиться в конечном автомате

2. текущий активный слой - это старший включённый бит в маске. если клавиша есть в конфиге этого слоя

3. на будущее: нужно написать ридми, потому что ты всё забудешь. Описать там, как всё работает, как именно всё захардкожено, что за что отвечает.
    - suspend_event это про suspend ивента синхронизации

4. нужно определять типа, есть ли в th_pending_t клавиша. и, если есть, отправлять пришедшую клавишу в tap-hold функцию.
   если нет pending, не отправлять в функцию, а просто отправить как NORMAL клавишу

5. layout.h - объявление массивов и функции, layout.c - реализация функции, *_layout.c - реализация массивов

6. в postclassfy_key_type(), если keycode будет больше KEY_MAX и не будет переназначен моим кодом, UB

7. вообще я в принципе проверок не делаю, потому что тупо не может быть кейса
   это не может быть кейса, а просто фолбэк, который не нужно проверять. можно это потом будет уточнить в доках

8. проверь потом как будет работать poll(), если POLLHUP случится.
типа вот эта странная клавиша копайлот отправляет несколько кодов, и fread прочитает байты, но не все, а только размера структуры.
нужно проверить что после этого будет poll() возвращать при повторном вызове, потому что в пайпе останутся ещё данные, но соединение уже будет разорвано

9. таймстамп даже не передаётся в libevdev, поэтому {0, 0}

10. нужно подумать, как сделать отправку нескольких клавиш на одно нажатие, потому что сейчас у меня стоит lalt + ralt = ctrl + backspace.

11. клавиша будет того типа, который последний стоит в make_key_type_lookup.
    но если в ремапе для слоя есть клавиша, которая стоит в каком-то конфиге, то она будет как клавиша после ремапа по слою
    и не пойдёт в главную функцию

12. в timerfd_settime() не нужно хранить структуры. данные из неё копируются в ядре.

13. внутри epoll_ctl() event копируется

14. придумай другое имя для LAYERS_MAX_SIZE
    ещё переименуй handle_layer_key() (toggle_layer())

15. напиши 100% про то, что с типами этого key_batch_t. потому что это непонятно с первого раза по любому.

16. нужно проверять tap[0] или hold[0] == ev.keycode_raw и больше ничего нет в массиве. если они равны и в массиве больше ничего нет,
    то клавиша не переназначалась, и нужно поставить ev.remapped = false. в противном случае ev.remapped = true.
    проверка будет через приведение к uind64_t, поэтому нужно именно 4 размер batch.
    это нужно потому что, если так не сделать, то придётся точечно под раскладку выставлять клавиши:
        например A это A на tap и SHIFT на hold. если бы не было этого ремапа,
        то на физической A была бы именно A, а не нужная клавиша раскладки (qwerty A = graphite N)

17. -1 для очереди это значит, что в ней ничего нет. как только туда добавляется элемент, очередь становиться 0.
    очередь повышается только в event_to_q().
    после flush очереди её нужно вернуть в исходное состояние -1.

18. чтобы клавиши не залипали нужно отправлять все UP клавиши из стейта.
